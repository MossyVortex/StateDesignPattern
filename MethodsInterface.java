public interface MethodsInterface { // the interface was called State in the YouTube video but I think this name is much clearer since it contains methods and not states
    void payFail();
    void payOk();
    void enter();
    void pay();

    // add new methods here then go to each state and write an override implementation for each State! 
}
